import path from "path";

export const ROOT_PATH = path.dirname(__dirname);
export const DATASETS_PATH = path.join(ROOT_PATH, "datasets");
export const ARTIFACTS_PATH = path.join(ROOT_PATH, "artifacts");
